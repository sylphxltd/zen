{
  "name": "@rapid/tui-advanced",
  "version": "0.0.0",
  "description": "Advanced components for professional TUI applications - Window management, splitters, text editors, charts",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "bunup src/index.ts --format esm --dts --target node --splitting=false --external @rapid/runtime --external @rapid/signal --external @rapid/tui --external chalk --external string-width --external strip-ansi",
    "build:watch": "bunup src/index.ts --format esm --dts --target node --splitting=false --watch --external @rapid/runtime --external @rapid/signal --external @rapid/tui --external chalk --external string-width --external strip-ansi",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["rapid", "tui", "terminal", "cli", "advanced", "window", "splitter", "fullscreen"],
  "author": "Rapid Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SylphxAI/rapid.git",
    "directory": "packages/rapid-tui-advanced"
  },
  "dependencies": {
    "@rapid/runtime": "workspace:*",
    "@rapid/signal": "workspace:*",
    "@rapid/tui": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "bunup": "^0.16.10",
    "typescript": "^5.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
